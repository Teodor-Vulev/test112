apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: apps-root
  namespace: argocd
spec:
  project: default
  source:
    repoURL: hgit@github.com:Teodor-Vulev/test112.git
    targetRevision: HEAD
    path: manifests/bare-minimum-api   # 👈 this is the folder with the app YAMLs
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
